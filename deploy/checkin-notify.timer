[Unit]
Description=每天定时检查续期到期并推送提醒

[Timer]
# 每天香港/北京时间 09:00 触发，不依赖服务器本地时区
OnCalendar=*-*-* 09:00:00 Asia/Hong_Kong
# 错过了（如关机）开机后补跑
Persistent=true
AccuracySec=1min
Unit=checkin-notify.service

[Install]
WantedBy=timers.target
